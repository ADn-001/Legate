/**
 * Setup Step 1: Check-in Interval (/setup/checkin)
 * - Segmented selector for check-in interval (7, 14, 30, 60 days)
 * - Grace period selector (3, 7, 14, 30 days)
 * - Continue button
 */

export default function StepCheckin() {
  // TODO: Implement Check-in Step
  // - Heading: "How often should we check in?"
  // - Segmented selector: 7/14/30/60 days (default 30)
  // - Grace period selector: 3/7/14/30 days (default 7)
  // - Explanation text: "If we don't hear from you within [X] days + [grace] days..."
  // - Continue button → navigate to /setup/beneficiary
  return <div>Step 1: Check-in Interval</div>
}
