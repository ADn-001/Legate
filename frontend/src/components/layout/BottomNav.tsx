/**
 * Bottom Navigation Component
 * - 4 tabs: Vault, People, Security, Activity
 * - Icons + labels
 * - Active state styling
 */

export default function BottomNav() {
  // TODO: Implement BottomNav
  // - Tabs: Vault (lock), People (people), Security (shield-check), Activity (clock-history)
  // - Active tab: icon + bold label, slightly darker tint
  // - Icons from lucide-react or similar
  return <nav>Bottom Nav</nav>
}
