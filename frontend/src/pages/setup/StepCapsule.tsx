/**
 * Setup Step 3: First Capsule (/setup/capsule) - SKIPPABLE
 * - Same form as Create Capsule screen
 * - Save Capsule / Skip for now buttons
 */

export default function StepCapsule() {
  // TODO: Implement Capsule Step (skippable)
  // - Same form as /vault/capsules/new
  // - Title input
  // - Message textarea
  // - Media attachment area
  // - Beneficiary selector
  // - Release trigger selector
  // - "Save Capsule" button → POST /capsules, navigate to /setup/recovery
  // - "Skip for now" button → navigate to /setup/recovery
  return <div>Step 3: First Capsule (Skippable)</div>
}
